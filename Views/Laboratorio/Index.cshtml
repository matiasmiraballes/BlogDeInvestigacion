@model IEnumerable<BlogDeInvestigacion.Models.Laboratorio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>LABORATORIOS</h2>

<p>
    @Html.ActionLink("Crear Laboratorio", "Create")
</p>
<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">@Html.DisplayNameFor(model => model.Nombre)</th>
            <th scope="col">@Html.DisplayNameFor(model => model.Descripcion)</th>
            @if (User.IsInRole("Administrador"))
            {
                <th scope="col"> ADMINISTRAR </th>
            }
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.Nombre, "Laboratorio", new { id = item.IdLaboratorio })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @if (User.IsInRole("Administrador"))
                    {
                        @Html.ActionLink("Edit", "Edit", new { id = item.IdLaboratorio })
                        @Html.Raw("|");
                        @Html.ActionLink("Details", "Details", new { id = item.IdLaboratorio })
                        @Html.Raw("|");
                        @Html.ActionLink("Delete", "Delete", new { id = item.IdLaboratorio })
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
